<% if notification.checked == false %>
  <%= link_to "既読", notification_path(notification), method: :patch, class: "btn btn-sm btn-primary" %>
<% else %>
  <%= link_to "削除", notification_path(notification), method: :delete, class: "btn btn-sm btn-danger" %>
<% end %>
