<div class="text-center">
  <div class="deck-status page-title">
    <% if @deck.id == current_user.id %>
      <h3><span style="<%= status_color(@deck) %>"><%= deck_status(@deck) %></span>のデッキ</h3>
    <% else %>
      <strong>
        <%= link_to user_path(@deck.user.id) do %>
          <%= image_tag @deck.user.icon.url, class: "icon-size" %>
          <%= @deck.user.nickname %>
        <% end %>
        さんのデッキ
      <% end %>
    </strong>
  </div>
  <div class="deck-list-image">
    <%= image_tag @deck.deck_list.url, class: "deck-image" %>
  </div>
  <div class="deck-free-space">
    <%= simple_format (h @deck.content) %>
  </div>
  <% if @deck.user_id == current_user.id %>
    <div class="container">
      <div class="row">
        <div class="col">
          <%= link_to "編集", edit_deck_list_path(@deck), class: "btn btn-success form-control" %>
        </div>
        <div class="col">
          <%= link_to "削除", deck_list_path, method: :delete, data: { confirm: "削除しますか？" }, class: "btn btn-danger form-control" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
